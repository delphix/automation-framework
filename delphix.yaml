template: js_template
api_key: app_key_token in KMS
ami: ami-0dfffed98347ecd
environments:
  - production:
      branch: master
      datapod: production-src
  - staging:
      branch: staging
      datapod: staging-pod
      ami: ami-2deacb3345ffed
      when:
        - push:
          then: refresh
  - uat:
      branch: testing
      datapod: user-testing-pod
      when:
        - push:
          then: refresh
        - pull-request-create:
          then: create
        - pull-request-closed:
          then: delete
  - develop:
      branch: develop
      datapod: develop-pod
      when:
        - push:
          then: refresh
  - all:
      when:
        - build-failure:
          then: bookmark
        - bookmark-complete:
          then: undo
